#include <stdio.h> 
#include <stdbool.h>

int main ()
{ 
    bool mikeIsFat = true; // boolean logic

    printf("Is Mike Fat (1 is yes. 0 is no)? %i\n ", mikeIsFat);
    printf("%i\n", mikeIsFat + 10);
    return 0;
}